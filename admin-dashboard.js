(function(){
  let users=[];
  function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
  function mount(){
    if(document.getElementById('page-admin'))return;
    const page=document.createElement('section');page.className='page';page.id='page-admin';page.innerHTML='<div class="card"><div class="section-head"><div><h3>Gestão de usuários</h3><div class="muted">Visão administrativa das contas cadastradas.</div></div><button class="btn" id="admRefresh">Atualizar</button></div><div class="table-scroll"><table class="data-table"><thead><tr><th>Usuário</th><th>Perfil</th><th>Status</th><th>Cadastro</th><th>Último acesso</th><th class="num">Lançamentos</th><th class="num">Cartões</th></tr></thead><tbody id="admBody"></tbody></table></div></div>';document.querySelector('main.main').appendChild(page);
    const btn=document.createElement('button');btn.dataset.page='admin';btn.textContent='Gestão de usuários';btn.onclick=()=>{document.querySelectorAll('.page').forEach(p=>p.classList.toggle('active',p.id==='page-admin'));document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b===btn));document.getElementById('pageTitle').textContent='Gestão de usuários';document.getElementById('pageSubtitle').textContent='Contas cadastradas no sistema';load();};document.querySelector('.nav').appendChild(btn);document.getElementById('admRefresh').onclick=load;
  }
  function render(){const body=document.getElementById('admBody');body.innerHTML=users.map(u=>`<tr><td>${esc(u.email||'Sem e-mail')}</td><td>${u.role==='admin'?'Gestor':'Usuário'}</td><td>${u.active?'Ativo':'Bloqueado'}</td><td>${u.created_at?new Date(u.created_at).toLocaleString('pt-BR'):'—'}</td><td>${u.last_sign_in_at?new Date(u.last_sign_in_at).toLocaleString('pt-BR'):'—'}</td><td class="num">${Number(u.transactions_count)||0}</td><td class="num">${Number(u.cards_count)||0}</td></tr>`).join('')||'<tr><td colspan="7" class="empty">Nenhum usuário encontrado.</td></tr>';}
  async function load(){const {data,error}=await sb.rpc('admin_list_users');if(error)return;users=Array.isArray(data)?data:[];render();}
  async function init(){if(!currentUser?.id)return;const {data}=await sb.from('user_profiles').select('role').eq('user_id',currentUser.id).maybeSingle();if(data?.role==='admin')mount();}
  let i=0;const timer=setInterval(()=>{i++;if(currentUser?.id){clearInterval(timer);init();}else if(i>40)clearInterval(timer);},250);
})();